# Neural tajweed service (isolated venv, Python 3.12). Recreate with:
#   python3.12 -m venv .venv312 && .venv312/bin/pip install -r requirements.txt
torch
torchaudio
transformers>=4.49,<5
librosa
soundfile
quran-transcript
fastapi
uvicorn
