# Impact map — number-one Kurdish Quran AI execution

**Status:** planning baseline; implementation blocked until `plan.md` approval.
**Grounding:** refreshed repository inspection plus the existing Serena-derived
`readiness-recovery-10-10/impact-map.md` and `lean-flutter-node-consolidation/impact-map.md`.
Serena currently refuses QrAi because this task is attached to the Codystem workspace; every implementing
task must reactivate Serena on QrAi and append any additional callers before editing code.

| Boundary / symbols | Known callers and consumers | Change obligation | Regression proof |
|---|---|---|---|
| Git/task truth: active `tasks.md` files, `scripts/update-ledger.sh`, `scripts/verify.sh` | developers, CI, cutover/release summaries, every implementation agent | map 76 active IDs exactly once; reject stale/duplicate/unmapped completion | master-ledger reconciliation, canonical gate, mutation fixture |
| Release evidence: `release-manifest.mjs`, `release-build-evidence.mjs`, `release-evidence-summary.mjs`, `smoke-evidence.mjs`, `release-challenge.mjs` | release workflows, canary controller, protected release gate, independent verifier | bind exact source/artifact/image/model/data/evaluator/SBOM/environment/signature/expiry | existing release suites plus final-manifest and adversarial stale/tamper cases |
| Release images/topology: `release-images.mjs`, `release-deployment.mjs`, Compose overlays, CI workflows | HTTP/realtime canaries, rollback, SBOM/license scan, runbooks | publish/consume immutable digests; no null/mutable identity | release artifact consumption, deployment selection, source-built image proof |
| HTTP canary: `http-canary-controller.mjs`, `lib/http-canary-*`, `server/src/routes/canary.mjs` | nginx target selection, Node/Rust observation, load evidence, rollback probes | execute candidate-bound live canary and automatic stop/reversal | HTTP canary effects/image/controller/monitoring/release-evidence tests |
| Node composition: `server/src/app.mjs::createApplication`, `main.mjs`, `worker.mjs`, `realtime/main.mjs` | API, worker, realtime images; lifecycle/health; final topology | converge final processes without CPU/realtime work on API loop | standalone lifecycle, shutdown, final-topology, clean-image tests |
| Database: `server/src/lib/db.mjs::createDb` | auth, pilot, review, inference, recitation, sessions, reports, progress, privacy and jobs via tenant transactions | preserve restricted role, transaction-local tenant, deadlines and pool-leak refusal | live RLS, cross-tenant, DB-role, timeout, restore and mutation tests |
| Realtime tickets: `server/src/lib/ticket.mjs` public ticket functions | recitation routes, Flutter/Web clients, Node/Rust gateways, gateway smoke/reconnect | preserve signature/tenant/retention/expiry/origin/single-use contract | shared vectors, replay, hostile input, multi-instance and canary rollback |
| Realtime flow: `server/src/realtime/{admission,audio,outcomes,protocol,replay}.mjs` | `StreamingRecorder`, chunk/object storage, inference jobs, teacher audio index | bounded backpressure/recovery, durable replay, explicit loss/degraded outcomes | protocol parity, load/soak, reconnect, retention, chunk-loss/index tests |
| Object storage: `audio-object-store.mjs`, `audio-index.mjs`, retention/privacy routes | realtime audio, teacher playback, export/delete, backup/restore | tenant-scoped private keys, durable index, retry-safe deletion/export | audio lifecycle, object isolation, erasure/reappearance, restore tests |
| Learner gate: `canShowLearnerFacingAiOutput`, `clearsLearnerFeedbackGate`, Flutter/Web gate adapters | inference response, persisted findings, review routes, learner panels/inbox | require reviewed acoustic provenance and calibrated evidence everywhere | gate parity, withheld-feedback, no-invented-confidence, Flutter corpus tests |
| Quran data: corpus/translation manifests, importers, word timings | Quran routes, Flutter/Web reader/practice, alignment, offline packages, restore | versioned byte-exact bundles; never normalize; truthful translation coverage | full corpus/NFC/provenance/import/reader/practice/package integrity tests |
| Web locale oracle: `localeCapabilities`, `getSelectableInterfaceLanguages`, `resolveSelectableInterfaceLanguage` | language selectors, persisted/URL locale, HTML lang/dir, Flutter governance migration | retain fail-closed policy until Flutter parity, then retire with permanent fixtures | locale expiry/key/no-fallback/RTL and retirement tests |
| Flutter root: `apps/flutter/lib/main.dart::QrAiApp` and shell/load state | all learner, teacher, scholar, privacy routes | become sole product surface after signed parity | app/widget/navigation/error/offline/signed-candidate tests |
| Flutter API/auth: `api_client.dart`, `models.dart`, `actor.dart`, `token_store.dart` | reader, practice, feedback, privacy, review, enrollment/session restoration | generated strict models; server-derived identity; protected storage and revocation | contract round-trip, canonical bytes, enrollment/401/logout/artifact scan |
| Flutter practice: `practice_screen.dart`, `consent_gate.dart`, `streaming_recorder.dart` | reader selection, realtime ticket/socket, playback, inference, progress | complete resilient listen/record/review/drill loop without duplicate writes | guided journey, recorder reconnect/batch, consent, progress idempotency, device loss |
| Flutter learner/staff: `tajweed_panel.dart`, `review_queue_screen.dart`, `privacy_screen.dart`, `mushaf_page.dart` | learner inbox, teacher review/edit/audio, scholar approvals, source/provenance, export/delete | complete role-specific reviewed journeys with accessible Kurdish copy | learner/teacher/scholar/privacy journey, lineage/redaction/audio seek tests |
| ASR service: `server.py`, `forced_align.py`, `acoustic_tajweed.py`, `readiness.py` | Node inference runtime, worker jobs, readiness, candidate evaluator | pin real candidate, bounded spans/windowing/capacity, abstain if unavailable | real-audio spans, readiness, crash/overload, canonical marks, acoustic shadow tests |
| Model evidence: `evaluate_candidate.py`, `candidate_evidence.py`, `eval_metrics.py`, calibration registry | release model verifier, learner gate threshold, model card, canary stop rules | immutable corpus/candidate/calibrator identity and subgroup/confidence bounds | candidate/model evidence, calibration, subgroup, tamper and reproduction tests |
| Theological tutor (new, approved-corpus boundary only) | Kurdish lessons, question answering, scholar escalation, audit/correction log | deterministic sacred quotes, claim citations, risk classification and abstention | exact quote/citation, unsupported/disputed/fatwa, injection and scholar gold set |
| Privacy/security policy: RLS migration, identity routes, privacy jobs, logs, headers/config | every API/worker/storage/client/deployment boundary | complete threat mappings, child controls, supply-chain and external assessment | production isolation, ASVS/MASVS policy, secret/raw-audio redaction, child red team |
| Operations: monitoring, `load-test.js`, backup/restore scripts, staging/operations runbooks | SLO alerts, canary stop, incident response, PITR/DR, independent challenge | prove capacity, ownership, encrypted restore and timed rollback | load/soak/chaos, alert route, backup crypto, PITR, rollback evidence validators |
| Retirement: Expo, React/nginx, Rust APIs/gateway/shared-ticket, agents, old specs | commands, workflows, Compose, imports, tests, docs and rollback oracles | remove only after replacement parity and observation; retain neutral fixtures | retired-components, final-topology, lean-tree, living-docs, spec-retirement gates |

## Change-control rule

Before each code task, the agent records the exact declaration and all references returned by Serena in the
task evidence. If a caller is absent from this table, add it before the failing test. No broad program row is
permission for an unmapped refactor.
