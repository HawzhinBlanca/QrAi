# Acoustic evaluation and calibrated learner gate — impact map

## Symbols and callers

| Symbol / data | Current callers | Planned impact / proof |
|---|---|---|
| `eval_metrics.py::{average_precision,precision_recall_f1,expected_calibration_error,cluster_bootstrap_ci,krippendorff_alpha_nominal}` | `test_eval_metrics.py`; new evaluator only | Reuse unchanged or extend with known-answer tests; evaluator computes from row-level evidence. |
| `evaluate_candidate.py` + evidence schema/verifier | canonical Python gate; future release verifier | Offline CLI + `test_eval_pipeline.py`; release verifier consumes its exact unsigned payload. |
| `model-evaluation-evidence-v1.schema.json` | new evaluator, verifier, persistence contract | Strict JSON Schema 2020-12 bundle; canonical contract test rejects aggregate-only, unknown, and uncalibrated release-shaped evidence. |
| `model-evidence-verifier.mjs::{canonicalizeRfc8785,verifyModelEvidenceBundle}` | `model-evidence.test.mjs`; release claim checker; smoke | Isolated verifier validates the strict bundle, exact RFC 8785 payload hash, Ed25519 signature, unique trusted key, and signer class, then returns an immutable clone of the verified payload. |
| `model-evaluation-trusted-signers-v1.json` | model-evidence verifier; release claim checker | Public JWK trust anchors only; committed production policy starts empty, so no repository artifact can independently become release-trusted. |
| `server.mjs::createEvalRun` | private `route` POST `/v1/eval-runs`; named export; `server.test.mjs`; `smoke-ml.mjs` | Retire copied-metric authority and its route/export; smoke invokes the offline evaluator/verifier contract instead of accepting service-authored aggregates. |
| `golden-regression.test.mjs` | standalone only | Add to `scripts/verify.sh`; permanent non-release eligibility assertions. |
| `verify-invocations.test.mjs` | new canonical contract guard | Prove both itself and the golden regression remain in the active Node verification command. |
| `ModelEvalRun` + `modelEvalPassesReleaseGate` | `platform-contracts.test.ts`; `canonical-gates-fixtures.test.ts`; `check-model-eval-claims.mjs` | Accept a verifier result as a separate authority; require release trust, exact payload/DB identities, counts/slices/calibration, uncertainty, and task-specific metric gates. |
| `scripts/check-model-eval-claims.mjs::{claimProblem,checkDatabase}` | canonical model-claim guard and its `--self-test`; direct module entrypoint | Read every tenant-visible row rather than newest-only, reconstruct/verify the detached bundle with operator trust, ignore ordinary ineligible history, and fail on invalid release-labelled or multiple distinct qualifying evidence. |
| `selectUniqueReleaseAuthority` | new `model-claim-authority.test.mjs`; `checkDatabase` | Pure selection policy proves a newer ineligible row cannot hide valid evidence, while invalid release-labelled and multiple distinct valid identities fail closed. |
| `eval_runs` | migration seeds; Rust/Node reports; checker; smoke SQL; RLS; API parity | Additive 0031 fields/backfill, unchanged tenant RLS, schema-equivalence and migration tests. |
| `0031_evaluation_evidence_authority.sql` | migration manifest/runner; fresh and legacy adoption; new `eval-evidence-migration.test.mjs`; future report writers | Add nullable signed-evidence/finding provenance, explicit legacy-fixture classification, immutable fields, exact same-tenant attribution FK, and no replacement table or RLS policy. |
| `0032_demote_untrusted_model_claims.sql` | migration manifest/runner; schema equivalence; `eval-evidence-migration.test.mjs`; live claim checker | Demote historical aggregate claims after production trust was deliberately initialized empty; future promotion requires the verified release workflow. |
| `0033_acoustic_finding_evidence_authority.sql` | migration manifest/runner; schema equivalence; seed history; direct acoustic inserts in Rust integration/API parity/migration tests; Node `persistTajweedFindings` | Add a historically scoped insert/attribution-update trigger: every new acoustic row carries the complete 0031 chain and references one same-tenant, acoustic-task, release-eligible eval row for the same model. Existing null historical rows remain reviewable/readable and withheld. |
| Rust `types.rs::EvalRun`, `eval.rs::get_eval_run` | Axum route registration in `lib.rs`; `/v1/eval-runs/{model_version}` integration and A/B parity suites | Return exact expanded shape; integration/API parity. Serena has no Rust language server in this workspace, so Rust references are grounded by `rg` plus compiler/tests. |
| Node `reports.mjs::getEvalRun` | `routes/index.mjs` registry import/handler; report parity, DB endpoint, authz and hostile-input suites | Match Rust declaration order and evidence fields; report parity and hostile input. |
| Web `platform.ts::{EvalRun,fetchEvalRun}` | `fetchBenchmarkMetrics` → `PlatformCommand` model-ops UI | Remove fabricated smoke success; the declared smoke fixture remains visibly ineligible and the UI renders eligibility/provenance honestly. |
| OpenAPI `EvalRun` and canonical gate JSON | validators, generated Flutter boundary, enum/DB parity | Strict new fields; fixture-ineligible and tamper cases. |
| `TraceableRecord`, `TajweedFinding`, `canShowLearnerFacingAiOutput` | `TajweedPredictionResponse`; `packages/contracts/tests/{canonical-gates-fixtures,platform-contracts}.test.ts`; Web `learnerVisibleTajweedFindings`/`tajweedReviewBadge`; agents gate compatibility | Preserve the generic basis-optional agent-run gate, but require complete evidence/model/dataset/calibrator/span/audio/audit provenance whenever `analysisBasis=acoustic`; execute one expanded corpus. |
| Node `clearsLearnerGate` | `redactWithheldFindings` → `proxyMl`; `tests/node-api/authz.test.mjs` corpus runner | Match the expanded contract; malformed sources, missing spans/audio, fixture-bound evaluation, missing audit/provenance, and explicit `withheld` all redact. |
| Node `persistTajweedFindings` | `proxyMl` after Tajweed response | Database enforcement will reject its current null provenance; T9 replaces sole-model guessing/clamping with exact verified response attribution and persists the full chain. Until then production `findings[]` remains empty. |
| Node `requireTajweedSemantics` | `proxyMl` before persistence/redaction | Reject non-finite/out-of-range confidence and every non-empty acoustic finding that lacks the exact model, artifact, dataset, calibrator, and evaluation identities needed by persistence. |
| Rust `review.rs::clears_learner_gate` | `ml_proxy.rs::redact_withheld_findings`; `review.rs::list_session_tajweed_findings`; local learner-gate tests and shared-corpus runner | Match the expanded corpus and consume joined alignment/audio/evaluation provenance; the learner read returns the complete gate input while redacting judgment fields on failure. |
| Rust `ml_proxy.rs` Tajweed validation/persistence | public ML proxy and integration tests | Validate exact attribution chain before writing any acoustic finding. |
| Dart `TajweedFinding.fromJson/isLearnerVisible` | `TajweedPanel`; practice session readback; review queue/audio UI; `tajweed_gate_test.dart` | Strictly parse provenance without inventing defaults; execute the shared corpus and fail closed on incomplete historical/readback rows. |
| Web `api.ts::TajweedFinding` + `tajweedReview.ts::{learnerVisibleTajweedFindings,tajweedReviewBadge}` | `TajweedPanel`; `PracticeFlow`; `App` practice state; Web Tajweed tests | Carry the complete provenance shape and delegate visibility/badge state to the shared TypeScript learner gate. |
| OpenAPI `PredictedTajweedFinding`, `SessionTajweedFinding`, `StaffTajweedFinding` | schema/completeness tests; Rust/Node parity fixtures; Flutter API boundary | Declare exact non-null producer fields for new predictions and nullable historical provenance for stored rows; every session response contains the complete learner-gate input. |
| Rust/Node staff and session finding SQL | staff review queue, learner-owned session readback, audio playback, API parity/E2E | Join `word_alignments`, earliest overlapping `audio_chunks`, consent, model version, audit, and exact evaluation provenance; never synthesize missing history. |
| `predictTajweed` / `runAcousticShadow` | private ML route; proxy; acoustic/session tests | Remain empty-findings shadow path until approved calibrator registry resolves exactly. |
| `model_attribution.py` acoustic/calibrator envelope | ASR response + attribution tests; Rust/Node validators | Bind future calibrator/evidence ids; current unavailable calibrator remains explicit. |
| New `calibration_registry.py` loader/resolver + `calibrator-registry.json` | `acoustic_tajweed.load_shadow_candidate`; Python acoustic/attribution tests; Node shadow contract test | Validate a closed registry, verify exact calibrator bytes, and require scorer-artifact + dataset-manifest + evaluation-evidence digest equality. Production registry begins with no active calibrator. |
| Python `load_shadow_candidate` | `server.py` module startup, `worker_main`, `test_acoustic_tajweed.py` | Load the production calibrator registry and refuse any active calibrator while the selected candidate remains `shadow-only`/ineligible. Tests exercise no-active, malformed, active, and binding-mismatch cases. |
| Python `build_acoustic_attribution` | `server.py::_acoustic_response`; `test_model_attribution.py` | Continue returning an explicitly unavailable calibrator for the production shadow candidate; no calibrated identity is inferred from the existence of raw scores. |
| Node `validateAcousticObservationResponse` / `runAcousticShadow` / `predictTajweed` | private ASR call, audit-only summary, `session-transcript.test.mjs`, `acoustic-shadow.test.mjs` | Reject any response claiming a calibrator or calibrated observation and keep public `findings` empty while the committed registry has no active authority. |
| `tajweed_findings` + linked `word_alignments` | Node/Rust persistence, queue/readback, teacher review, privacy | Add provenance columns; use alignment span/audio evidence; retain RLS/privacy behavior. |

## Required verification set

- Python: `test_eval_metrics.py`, new `test_eval_pipeline.py`, acoustic/attribution suites.
- Contract/release: new `learner-feedback-gate.test.mjs`, `model-evidence.test.mjs`, canonical gate,
  OpenAPI completeness, no-invented-confidence, golden regression invocation guard.
- Database/API: migration runner, schema equivalence, restricted-role/RLS, reports parity,
  Tajweed persistence effects, review/readback/redaction, privacy erasure.
- Flutter: Tajweed parser/gate/panel/practice/review suites.
- Canonical: `bash scripts/verify.sh`; protected `--release` only with owner-supplied trusted evidence.
