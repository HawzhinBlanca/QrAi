# Database URL Redaction Tasks

- [x] T1 Redact credential-bearing database URLs before startup logs print them. Tests: `cargo test --manifest-path services/platform-api/Cargo.toml`, `bash scripts/verify.sh`
