# W2.2 evidence — server package boundary

**Status:** complete; canonical verification and required remote checks are green, and the ledger is closed<br>
**Date:** 2026-08-08

## Implemented seam

- `server` is a pnpm workspace package named `@quran-ai/server`, ESM-only on Node 22.13 or newer.
- Its production dependency boundary declares the S3 client, Fastify/CORS, JOSE, `postgres`, and
  `pg`; TypeScript and Node types are development-only package tooling.
- `server/src/app.mjs::createApplication` is the single side-effect-free application seam.
- Importing the package does not bind a socket. Construction, readiness, injection, and close are
  independently testable.
- Root and canonical type/build workflows include the server package, and canonical verification
  invokes the standalone lifecycle test exactly once.

W2.2 established the package and composition boundary. Later, separately proved W2 tasks moved route
behavior into that same package, so this closure does not repeat the obsolete claim that the current
composition root delegates to `services/node-api`. W2.2 makes no production-image claim; W2.3 owns
that proof independently.

## Focused proof

These commands were rerun against the closing tree:

```text
pnpm install --frozen-lockfile
Scope: all 5 workspace projects
Already up to date

pnpm --filter @quran-ai/server typecheck
$ tsc --project tsconfig.json

pnpm --filter @quran-ai/server build
$ pnpm run lint && pnpm run typecheck
$ node --check src/app.mjs src/main.mjs [...] src/realtime/*.mjs [...] src/storage/*.mjs
$ tsc --project tsconfig.json

node --test tests/node-api/standalone-lifecycle.test.mjs
# tests 4
# pass 4
# fail 0
```

The consolidated repository history does not expose a reproducible pre-package commit, so this
closure does not preserve the former unverified “3/3 red-first” assertion. It records only commands
and outcomes reproduced on the closing tree.

## Canonical proof

The ledger updater reran the focused proof and the full live-Postgres canonical gate before changing
W2.2 from open to done:

```text
PATH=/Users/hawzhin/flutter/bin:$PATH \
MIGRATION_TEST_ADMIN_URL=postgresql://hawzhin@127.0.0.1:5433/quran_ai \
scripts/update-ledger.sh W2.2 tests/node-api/standalone-lifecycle.test.mjs
VERIFY OK
Ledger updated: W2.2 done (tests: tests/node-api/standalone-lifecycle.test.mjs; verify.sh passed).
```

Required remote checks also passed on exact branch SHA
`739c28ad2a4f5ec10bcee40e3a7c5ac369cab7e2`, where the W2.2 package boundary and focused proof are
unchanged:

- [`ci` run 31237891063](https://github.com/HawzhinBlanca/QrAi/actions/runs/31237891063):
  `verify`, mutation, smoke, `node-min`, and `android` passed;
- [`docker-build` run 31237891057](https://github.com/HawzhinBlanca/QrAi/actions/runs/31237891057):
  `build` passed.

Only W2.2 is closed by this evidence. W2.3 must separately prove the locked, non-root production
image and internal shadow service.
