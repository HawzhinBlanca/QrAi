# W2.17 T1 evidence — server-owned inference boundary

**Date:** 2026-08-07<br>
**Scope:** T1 only — ownership guard, behavior-preserving module/test relocation, canonical-byte and
output parity. W2.17 and required remote CI remain open.

## Red proof

- `tests/contract/inference-module-boundary.test.mjs` initially failed all three assertions because
  the server-owned inference modules, fixture, and relocated test boundary did not exist.
- The hardened production-image test initially failed two assertions because both Dockerfiles still
  copied the old service-owned graph.
- The first full canonical run exposed two additional boundary failures: the relocated audit code
  named tenant identifiers in error logs, and the standalone package assertion omitted the new
  `src/inference/*.mjs` lint surface. Both were fixed without weakening an assertion.

## Implemented boundary

- The executable alignment, Tajweed, attribution, runtime, and golden fixture now live under
  `server/src/inference/`.
- ML behavior tests now live under `tests/inference/`; the old service entrypoint is only a temporary
  compatibility launcher.
- Verification, proof, contract scanners, ASR mirrors, package linting, and Docker packaging point at
  the server-owned graph.
- Canonical Quran text remains byte-preserved; no canonical-source `.normalize()` call was added and
  the repository-wide escaped Arabic-regex scanner covers tracked and untracked source.
- Audit failure logs reveal neither tenant identifiers nor storage-derived error details.

## Green proof

- Relocated inference suite: 123 tests, 122 passed, one pre-existing scholar-review TODO, zero failed.
- Focused boundary/security/image suite: 18 passed, zero failed after the canonical findings were
  repaired.
- Affected contract suite: 58 passed, zero failed.
- Python forced-alignment invariant: 6 passed, zero failed.
- Server lint, typecheck, and build: passed.
- `git diff --check`: passed.

Exact canonical command:

```sh
set -a
source scripts/stack.env
set +a
MIGRATION_TEST_ADMIN_URL='postgresql://hawzhin@127.0.0.1:5433/postgres' \
  PATH="/opt/homebrew/Cellar/postgresql@16/16.14/bin:$PATH" \
  bash scripts/verify.sh
```

Result: `VERIFY OK` (exit 0). This included the 743-test Node-services gate, live Postgres migration
and RLS integrations, 389-test Rust/Node parity, 332-test Node-port parity, gateway/audio E2E,
production builds, license/security/model-evidence guards, and ASR/Quran invariants. Flutter remained
an explicit SDK-unavailable skip, so required remote CI is not claimed.
