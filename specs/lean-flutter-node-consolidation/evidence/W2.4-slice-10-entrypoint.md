# W2.4 slice 10 evidence — consolidate composition and process startup

**Status:** complete; local, image, and exact-candidate remote proof are green<br>
**Date:** 2026-08-08

## Scope

- Removed `services/node-api/server.mjs`, the final JavaScript owner under the transitional tree.
- Made `server/src/app.mjs::createApplication` the sole side-effect-free Fastify composition root.
- Added `server/src/main.mjs` as the sole process entrypoint for environment validation, the literal
  portable allowlist, socket binding, and startup exit policy.
- Retargeted every executable import/read/spawn, canonical route derivation, cutover check, Docker
  command, and current assurance citation.
- Removed the production image's `services/node-api` copy entirely.
- Changed no middleware, route, response, error, secret, CORS, tenant, proxy, or traffic behavior.

## Red-first and focused proof

The composition/entrypoint ownership test failed in exactly two tests before implementation: the
legacy owner still existed and executable consumers still named it. After the split, the first
focused run exposed only static expectations tied to the old cross-package imports; all runtime
shell, CORS, proxy, boot, and NUL behavior was already green. After correcting and pinning the new
boundary:

```text
node --experimental-strip-types --test \
  tests/node-api/module-relocation.test.mjs \
  tests/node-api/standalone-lifecycle.test.mjs \
  tests/node-api/routes-table.test.mjs \
  tests/node-api/shell.test.mjs \
  tests/node-api/no-secret-logging.test.mjs \
  tests/node-api/boot-guard.test.mjs \
  tests/node-api/nul-byte-is-a-400.test.mjs
# tests 75
# pass 75
# fail 0

pnpm --filter @quran-ai/server build
# exit 0

git diff --check
# exit 0
```

The first canonical run then found one stale current-assurance citation in
`specs/cutover/boundary.md`. It was retargeted and added to the relocation guard; the focused
ownership, boundary-integrity, and image-contract rerun passed 31/31.

## Production-image proof

The source-built image reached healthy, ran as non-root, loaded the package-owned app and
entrypoint, exposed the exact 37 portable routes, and contained no `services/node-api` path:

```text
health=healthy
user=node
image=sha256:f7b941dbc5a30ef2c6111e24f30804f468c1209da8c199e5455c7ea67787f07e
package-owned app/main loaded; legacy tree absent; 37 portable routes
probe_container_removed=yes
```

This is local engineering evidence, not a release digest.

## Canonical proof

```text
MIGRATION_TEST_ADMIN_URL=postgresql://hawzhin@127.0.0.1:5433/postgres bash scripts/verify.sh
# canonical Node tests 643; pass 641; fail 0; skipped 1; todo 1
# direct API parity tests 369; pass 369; fail 0
# through-Node API parity tests 319; pass 319; fail 0
VERIFY OK
```

The counts above are the historical slice-10 proof from 2026-08-07. Later approved additive work
expanded the executable registry from 37 to 44 routes without reintroducing a second owner. The
current closure proof on 2026-08-08 was:

```text
node --experimental-strip-types --test \
  tests/node-api/module-relocation.test.mjs \
  tests/node-api/standalone-lifecycle.test.mjs \
  tests/node-api/route-registry.test.mjs \
  tests/node-api/shell.test.mjs \
  tests/node-api/no-secret-logging.test.mjs \
  tests/node-api/boot-guard.test.mjs \
  tests/node-api/nul-byte-is-a-400.test.mjs
# tests 90
# pass 90
# fail 0

MIGRATION_TEST_ADMIN_URL=postgresql://hawzhin@127.0.0.1:5433/quran_ai \
  bash scripts/verify.sh
# Node tests 831; pass 829; fail 0; skipped 1; todo 1
# direct API parity 389/389
# through-Node API parity 332/332
VERIFY OK
```

Exact source candidate `f39b23cd2f5c11b1296ffe920ce215f54c749df7` then passed all required
remote checks on PR #388:

- `verify` — passed, including canonical verification, mutation proof, and CI smoke;
- `node-min` — passed;
- `android` — passed, including release APK identity validation;
- `build` — passed, including the production-image runtime checks.

`rg --files services/node-api` returns no files. The server package is the only Node API module,
composition, route, and process-entrypoint owner; the explicit compatibility proxy remains the
bounded rollback mode. This closes W2.4 without claiming that later backend tasks are complete.
