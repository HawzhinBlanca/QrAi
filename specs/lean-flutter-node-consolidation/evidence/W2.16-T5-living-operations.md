# W2.16 T5 evidence — living operations and final local gate

**Local status:** complete on 2026-08-07<br>
**Umbrella status:** open pending required remote CI and later native/release gates

## Closed documentation and deployment boundary

- ADR-0038 and the architecture now describe the implemented migration 0035/device-session
  boundary rather than calling durable enrollment future work.
- Testing, data inventory, staging operations, and the threat model now agree on a 24-hour
  invitation, 15-minute access lifetime, seven-day idle lifetime, 30-day absolute family lifetime,
  rotation/replay-family revocation, hash-only storage, and count-only privacy export.
- The staging runbook documents restricted admin provisioning, one-time secret delivery,
  default-off canary activation, refresh-replay incident response, and nondestructive rollback.
- Compose now passes `DEVICE_IDENTITY_ENABLED` to `node-api` with an explicit default of zero. This
  fixed a real operational gap: the server gate existed but the normal deployment could not enable
  an approved canary without an ad-hoc override.
- The existing canonical decision guard now reads every affected living document, and the
  production-image test pins the default-off Compose wiring.

## Red and focused proof

- The new living-document test failed first because ADR-0038 lacked a W2.16 implementation note and
  the architecture still classified durable enrollment as future work.
- The production-image test failed first because Compose did not pass
  `DEVICE_IDENTITY_ENABLED`.
- After correction, the combined decision/production-image group passed 10/10 tests.
- `git diff --check` passed.

## Final canonical proof

The exact live-Postgres repository gate ran after the documentation and Compose correction:

```sh
set -a
source scripts/stack.env
set +a
MIGRATION_TEST_ADMIN_URL='postgresql://hawzhin@127.0.0.1:5433/postgres' \
  PATH="/opt/homebrew/Cellar/postgresql@16/16.14/bin:$PATH" \
  bash scripts/verify.sh
```

Result: `VERIFY OK`.

The run included the seven-case live enrollment/provisioning/privacy journey, device migration and
restricted-role proof, living-doc and production-image guards, 99 live Rust platform integrations,
389 direct API-parity tests, 332 through-Node parity tests, production builds, bundle-secret and
security-header checks, 552 dependency licences, and model-evaluation claim guards. The hostile
WebSocket process suite also passed 7/7.

Flutter analysis/tests were explicitly skipped because no Flutter SDK is installed. Remote CI is
not observable from this workspace and is not claimed green. W2.16 therefore remains unchecked in
the global consolidation ledger even though all five local implementation tasks are complete.
