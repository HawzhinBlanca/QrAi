# W2.4 slice 9 evidence — relocate the complete route layer

**Status:** engineering proof complete; W2.4 remains open for entrypoint relocation and remote CI<br>
**Date:** 2026-08-07

## Scope

- Moved the route index and all thirteen mutually coupled domain modules from
  `services/node-api/routes` to `server/src/routes`; the old route tree has no JavaScript owners.
- Collapsed every temporary cross-package library import to the canonical sibling `../lib` owner.
- Retargeted the ML attribution bridge and every executable source consumer of the route layer.
- Changed no handler logic, route key, method, path, body limit, middleware order, response, role,
  tenant, learner-feedback gate, or traffic target.

## Red-first and focused proof

The route-owner and source-consumer guards failed in exactly two tests before the move. The first
post-move run then exposed six stale paths inside older relocation assertions; correcting those
made the focused route/runtime proof green. The first canonical run found three additional static
consumers (`ml-findings-shape` and two `no-secret-logging` reads), proving that the source scans were
not allowed to become vacuous. After retargeting and adding them to the ownership guard:

```text
node --experimental-strip-types --test \
  tests/node-api/module-relocation.test.mjs \
  tests/contract/ml-findings-shape.test.mjs \
  tests/node-api/no-secret-logging.test.mjs \
  tests/node-api/routes-table.test.mjs
# tests 33
# pass 33
# fail 0

pnpm --filter @quran-ai/server build
# exit 0

git diff --check
# exit 0
```

The route guard pins the exact fourteen-file set, the route index exports, all 37 portable route
definitions, executable source consumers, and the absence of temporary cross-package library
paths.

## Production-image proof

The source-built image reached healthy, ran as non-root, and loaded the complete route composition
plus all thirteen domain modules:

```text
health=healthy
user=node
image=sha256:4950dac0338ab717625f694730fd596c4ab27ed22b1b476b2afa2f116b6e714a
37-route composition and 13 domain modules loaded
```

This is local engineering evidence, not a release digest.

## Canonical proof

```text
MIGRATION_TEST_ADMIN_URL=postgresql://hawzhin@127.0.0.1:5433/postgres bash scripts/verify.sh
# canonical Node tests 641; pass 639; fail 0; skipped 1; todo 1
# direct API parity tests 369; pass 369; fail 0
# through-Node API parity tests 319; pass 319; fail 0
VERIFY OK
```

All route modules now have one owner under the server package. W2.4 stays unchecked until the
legacy entrypoint is removed and required remote CI is green.
