# W2.15 T1 evidence — durable job schema

Date: 2026-08-07

## Implemented

- Added checksum-pinned migration `0034_background_jobs.sql` and manifest/schema-equivalence
  coverage.
- Added one tenant-scoped outbox/queue table with a closed kind/state vocabulary, bounded payload
  and result objects, idempotency uniqueness, ready/expired/dead indexes, attempt/lease generation
  fencing fields, and forced RLS.
- Proved that a restricted runtime role sees only its tenant and that concurrent
  `FOR UPDATE SKIP LOCKED` claims select distinct jobs.
- Added the table to contracts, migration inventories, SQL smoke, teardown, and canonical
  invocation guards.
- Repaired SQL smoke setup so it uses isolated transaction-local tenant fixtures, never deletes or
  rewrites canonical Qur'an rows, and models instructional text rules without fabricated acoustic
  confidence.

## Focused proof

- `job-outbox-migration.test.mjs`: 2/2 passed against live PostgreSQL.
- Migration/schema/restricted-role group: 10/10 passed.
- Contracts: 36/36 passed.
- Live SQL smoke: 18/18 tenant tables passed forced-RLS checks.

## Canonical proof

Command:

```sh
set -a
source scripts/stack.env
set +a
MIGRATION_TEST_ADMIN_URL='postgresql://hawzhin@127.0.0.1:5433/postgres' PATH="/opt/homebrew/Cellar/postgresql@16/16.14/bin:$PATH" bash scripts/verify.sh
```

Result: `VERIFY OK`.

Notable live results: 99/99 Rust API integration tests, 388/388 direct API parity tests,
332/332 Node-through-shell parity tests, real gateway/audio/model-provenance flows, production
build, security/license/model-claim guards, and the new migration proof all passed. Flutter was
explicitly skipped because its SDK is not installed. Required remote CI is not yet available, so
the umbrella W2.15 ledger remains open.
