# W2.17 T3 evidence — worker ingress, audio privacy, and retention lifecycle

**Date:** 2026-08-07<br>
**Result:** local canonical proof passed (`VERIFY OK`)<br>
**Scope:** W2.17 T3 only; the W2.17 umbrella and remote CI remain open.

## Implemented boundary

- The worker private listener exposes only the ML-key-gated `POST /v1/audio-chunks` compatibility
  ingress, with bounded input/deadline/rate budgets and create-only injected object storage.
- The Rust gateway real-process proofs now target that exact worker ingress handler, not the old ML
  service process, while preserving retention, retry, index-failure, and delivery-failure behavior.
- Privacy export/delete and teacher playback use the injected object store directly and fail closed
  when it is missing or times out. No ML HTTP storage/privacy fallback remains in Node routes.
- One cancellation-aware, non-overlapping retention sweep runs in the worker and participates in
  bounded shutdown before storage and database close.
- API and worker production entrypoints construct the same storage boundary; parity workers mount
  the same temporary store as their subject API shell.

## Red-to-green proof

Focused results:

- Compatibility ingress: 7/7 passed.
- Retention worker/lifecycle: 3/3 passed.
- Direct privacy/playback parity: 17/17 passed.
- Gateway-to-worker ingress: 5/5 passed.
- Direct-store timeout/fail-closed suite: 7/7 passed.
- Full through-Node parity projection: passed with no failures.

The canonical gate caught and repaired two obsolete harness assumptions: one timeout test still
expected an ML-HTTP parse error, and one contract-shape worker omitted the shared store. Both now
exercise the direct injected storage architecture.

## Canonical command

```sh
set -a
source scripts/stack.env
set +a
MIGRATION_TEST_ADMIN_URL='postgresql://hawzhin@127.0.0.1:5433/postgres' \
  PATH="/opt/homebrew/Cellar/postgresql@16/16.14/bin:$PATH" \
  bash scripts/verify.sh
```

Final output: `VERIFY OK` (exit 0). This included TypeScript/Rust/Python tests, live Postgres
integration, real gateway audio proofs, direct and through-Node parity, production builds, restore
and security guards, dependency licences, and model-evaluation claim checks.
