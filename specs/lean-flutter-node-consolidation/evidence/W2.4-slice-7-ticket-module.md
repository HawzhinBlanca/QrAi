# W2.4 slice 7 evidence — relocate the realtime ticket authority

**Status:** engineering proof complete; W2.4 remains open for the remaining module/routes and remote CI<br>
**Date:** 2026-08-07

## Scope

- Moved `services/node-api/lib/ticket.mjs` to `server/src/lib/ticket.mjs` and removed the old owner.
- Updated all eight runtime/test/smoke/chaos imports and three implementation citations.
- Preserved all six exports and the byte-level `rt_v2` dot-delimited HMAC-SHA256 contract.
- Changed no ticket field, order, signature, consent/retention meaning, expiry, nonce, replay behavior,
  API route, gateway protocol, or traffic target.

## Red-first and focused proof

The new authority-ownership and all-caller/reference guards failed in exactly two tests before the
move. After implementation:

```text
node --test \
  tests/node-api/module-relocation.test.mjs \
  tests/node-api/ticket-vectors.test.mjs \
  tests/node-api/production-image.test.mjs \
  tests/node-api/standalone-lifecycle.test.mjs
# tests 34
# pass 34
# fail 0

cargo test --manifest-path services/shared-ticket/Cargo.toml
# tests 19; pass 19; fail 0; ignored generator 1

cargo build --manifest-path services/realtime-gateway/Cargo.toml
node --test tests/gateway/ws-hostile-input.test.mjs
# tests 7
# pass 7
# fail 0

pnpm --filter @quran-ai/server build
# exit 0

rg 'services/node-api/lib/ticket\.mjs|from "\.\.?/lib/ticket\.mjs"' \
  services server tests scripts packages specs/gateway-ws-sweep specs/node-backend-port
# no stale scoped references

git diff --check
# exit 0
```

The proofs cover every committed cross-language vector; empty, dotted, null, non-boolean, unsafe
expiry, wrong-version, wrong-secret, wrong-session, expired, extra-part, tampered retention and
tampered signature cases; and a live gateway process that remains healthy after hostile frames.

## Production-image proof

The source-built image reached healthy with Rust intentionally unreachable, ran as non-root, and
used the deployed module to sign, verify, and reject a tenant-tampered ticket:

```text
health=healthy
user=node
image=sha256:b868751df786c08aa7f921128443384e494a9f2c526b2f47e6b52a09efb57add
rt_v2 sign/verify/tamper probe passed
```

The exact temporary container was removed. This is local engineering evidence, not a release
digest.

## Canonical proof

```text
MIGRATION_TEST_ADMIN_URL=postgresql://hawzhin@127.0.0.1:5433/postgres bash scripts/verify.sh
# canonical Node tests 637; pass 635; fail 0; skipped 1; todo 1
# direct API parity tests 369; pass 369; fail 0
# through-Node API parity tests 319; pass 319; fail 0
VERIFY OK
```

W2.4 remains unchecked until the remaining legacy Node module/routes move and required remote CI is
green.
