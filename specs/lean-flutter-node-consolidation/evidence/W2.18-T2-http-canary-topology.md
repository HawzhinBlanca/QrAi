# W2.18 T2 evidence — explicit HTTP canary topology

**Recorded:** 2026-08-08<br>
**Scope:** repository implementation and local live-Postgres verification only<br>
**Result:** PASS

## Implemented boundary

- `server/src/routes/canary.mjs` derives exactly 39 canary-owned method/path keys from the checked
  route manifest: the 38 retained baseline operations plus the implemented learner-history target
  addition. Count drift, duplicate keys, missing local handlers, and owner-gated routes fail closed.
- `NODE_API_ROUTE_MODE=retained-canary` accepts no copied `NODE_API_PORTED` list and requires the
  retained Rust upstream. The base `explicit-compatibility` mode remains the default.
- Base Compose keeps Web and gateway indexing on Rust. `docker-compose.canary.yml` moves both to
  Node together while Rust remains healthy for four retirement-transition routes and reversal.
- The Web image uses nginx runtime templates. Startup accepts only `platform-api:8080` or
  `node-api:8082`; missing, URL-shaped, and arbitrary upstreams are refused.
- Mutable retained requests execute locally once. Transition and Rust-only requests proxy once;
  no random split, shadow write, or dual execution exists.
- `docker-compose.release.yml` composes with the canary overlay without restoring a source-build
  fallback. Living architecture, decision, testing, and staging documents describe the same
  immutable reversal boundary.

## Automated proof

Focused suites passed:

```text
95/95  topology, effects, boot guards, production image, readiness, invocation, and headers
27/27  route ownership inventory plus topology/effect regression after the inventory correction
```

Both configurations validated with `docker compose ... config --quiet`:

```text
docker-compose.yml + docker-compose.canary.yml
docker-compose.yml + docker-compose.release.yml + docker-compose.canary.yml
```

`git diff --check` passed.

The first canonical run correctly failed because the complete route-layer ownership inventory did
not yet list the new `canary.mjs` module. `tests/node-api/module-relocation.test.mjs` exposed the
omission. The inventory was corrected, its focused suite passed, and the entire canonical gate was
then rerun from the beginning.

## Canonical gate

Exact command:

```bash
set -a
source scripts/stack.env
set +a
MIGRATION_TEST_ADMIN_URL='postgresql://hawzhin@127.0.0.1:5433/postgres' \
  PATH="/opt/homebrew/Cellar/postgresql@16/16.14/bin:$PATH" \
  bash scripts/verify.sh
```

Final result:

```text
99/99   live Rust platform API integration tests
389/389 direct Node/Rust API parity and effect tests
332/332 parity tests through the Node route layer
PASS     Rust fmt/clippy, TypeScript typechecks/tests, Python/Rust/Node suites
PASS     production build and Web bundle/header/secret guards
PASS     dependency licences and model-claim guard
VERIFY OK
```

## Honest limits

- No GHCR candidate was published or deployed by this local task.
- No running candidate image ID/digest, load/soak window, stop-condition drill, or rollback timing
  is claimed here; those are W2.18 T3-T5 evidence.
- Flutter is unavailable on this machine, so native Flutter analysis/device CI is not claimed.
- Remote CI and owner/security/operations approval remain required. The readiness checker therefore
  remains `NOT READY` even though traffic-share and rollback-artifact mechanics are now `MET`.
