# W3.2 evidence — isolated Node realtime process lifecycle

**Recorded:** 2026-08-08 05:24 +03<br>
**Branch:** `codex/lean-flutter-node-consolidation`<br>
**Pre-implementation base:** `6929d906b1db`<br>
**Implementation commit:** `bf21595f37d8734e0b5d724921aa975df8ca62c1`<br>
**Criteria:** RTP-1…RTP-6 / parent BE-5 and OP-4<br>
**Closure state:** local proof, exact-SHA CI, and guarded ledger closure passed.

## Red-first evidence

- The first `tests/realtime/process-lifecycle.test.mjs` run failed seven cases because
  `server/src/realtime/main.mjs` did not exist.
- The first topology/release/monitoring run failed 18 assertions because the third Node command,
  immutable release consumer, running-image evidence, rollback count, scrape, and alert did not
  exist.
- A deliberate no-upgrade-listener check left the raw upgrade socket open. The final process uses a
  fixed 404-and-close refusal listener with no WebSocket library, ticket parser, or admission path;
  the regression test pins both the refusal and the absence of W3.3 behavior.

## Implemented boundary

- One new side-effect-free `server/src/realtime/main.mjs` command in the existing package/image.
- Only `/health`, bounded deep `/ready`, and private fixed-cardinality `/metrics`; all application
  routes are absent and upgrades are refused.
- Pre-listen restricted-role and private-store checks; ongoing readiness covers Postgres, object
  store, worker, and loaded-model ASR without returning dependency detail.
- The existing bounded Fastify shutdown controller is reused with a validated fixed process label.
- Compose runs `node-realtime` internally from the same Node artifact, without a host port, Web edge,
  gateway edge, new package, new image key, dependency, migration, or traffic movement.
- Release/rollback evidence now observes eight running services and seven rollback application
  services; traffic reversal remains the exact Web/gateway/API sequence.
- Prometheus privately scrapes the shadow and raises a closed-label investigation warning while the
  Rust gateway remains the only realtime ingress and audio-loss oracle.

## Focused proof

The complete affected-caller command ran with live Postgres and passed:

- 117 tests passed, 0 failed, 0 skipped.
- The realtime lifecycle suite passed 8/8, including the actual entrypoint under a freshly
  provisioned restricted login role and clean SIGTERM resource exit.
- `pnpm --filter @quran-ai/server build` passed.
- Base and immutable-release Compose configurations rendered successfully with declared test-only
  placeholder values.

## Canonical proof

```sh
PATH=/Users/hawzhin/flutter/bin:$PATH \
MIGRATION_TEST_ADMIN_URL=postgresql://hawzhin@127.0.0.1:5433/quran_ai \
bash scripts/verify.sh
```

Result: exit 0, final line `VERIFY OK`. The run included Rust fmt/clippy, TS typecheck/tests, the
canonical Node suite, real gateway/audio processes, live migrations/RLS/API parity through both
Rust and Node, Python/ASR gates, all Rust suites, production builds, web secret/header guards,
dependency licences, and model-claim guards.

## Remote and ledger closure

The exact pushed implementation SHA `bf21595f37d8734e0b5d724921aa975df8ca62c1` passed all required
pull-request checks:

- `ci/node-min`
- `ci/android`
- `docker-build/build`
- `ci/verify`

Only after those checks were green, the guarded `scripts/update-ledger.sh W3.2 RTP-1` command ran
the canonical gate again, ended with `VERIFY OK`, and changed the W3.2 task row to checked. The
RTP-1…RTP-6 mapping and focused results above are the complete proof carried by that row; the
script's second argument is only its concise console evidence label.
