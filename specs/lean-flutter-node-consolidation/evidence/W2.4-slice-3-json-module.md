# W2.4 slice 3 evidence — relocate the Rust-wire JSON leaf

**Status:** engineering proof complete; W2.4 remains open for the remaining modules and remote CI<br>
**Date:** 2026-08-07

## Scope

- Moved `services/node-api/lib/json.mjs` to `server/src/lib/json.mjs` and deleted the old owner.
- Updated exactly eleven imports: the server reply serializer, six route modules, and four imports
  across the Rust-JSON and report/session parity tests.
- Preserved all six exports and every byte-level behavior: f32 narrowing, whole-float decimal form,
  non-finite refusal, random per-call sentinel, hostile payload safety, and recursive object-key
  ordering.
- Changed no schema, route, traffic target, canonical text handling, or proxy rollback behavior.

## Red-first and focused proof

The two new relocation assertions failed before the implementation because the legacy owner and all
callers were still present. Serena reviewed and then applied the exact 1 + 6 + 4 import changes.

```text
node --test tests/node-api/module-relocation.test.mjs \
  tests/node-api/rust-json.test.mjs \
  tests/node-api/shell.test.mjs \
  tests/node-api/production-image.test.mjs
# tests 43
# pass 43
# fail 0

node --test --test-concurrency=1 \
  tests/api-parity/reports-parity.test.mjs \
  tests/api-parity/sessions-parity.test.mjs
# tests 24
# pass 24
# fail 0

pnpm --filter @quran-ai/server build
# exit 0

rg 'services/node-api/lib/json\\.mjs|from "\\.\\.?/lib/json\\.mjs"' \
  services tests scripts server
# no stale references
```

## Production-image proof

The source-built image reached healthy as the non-root user while Rust was intentionally
unreachable. The in-container probe exercised `stringifyRust({z: f64(100), ...})` and
`sortKeysDeep` against exact expected bytes:

```text
health=healthy
user=node
image=sha256:1b7100ce100e3d6f27a0de1e32579239ac8ac74dc273bad80be5cb4c628cebf2
moved-json=exercised
```

The temporary container was removed. The image id is local engineering evidence, not a release
digest.

## Canonical proof

```text
MIGRATION_TEST_ADMIN_URL=postgresql://hawzhin@127.0.0.1:5433/postgres bash scripts/verify.sh
# targeted report/session parity tests 24; pass 24; fail 0
# direct API parity tests 369; pass 369; fail 0
# through-Node API parity tests 319; pass 319; fail 0
VERIFY OK
```

W2.4 remains unchecked until every remaining legacy Node module has moved and remote CI is green.
