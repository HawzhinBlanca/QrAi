# W2.4 slice 6 evidence — relocate the tenant-scoped database primitive

**Status:** engineering proof complete; W2.4 remains open for the remaining modules and remote CI<br>
**Date:** 2026-08-07

## Scope

- Moved `services/node-api/lib/db.mjs` to `server/src/lib/db.mjs` and removed the old owner.
- Updated the composition root, the live RLS oracle, and the cutover security-boundary citation.
- Preserved the single `createDb` export and returned `{sql, withTenant, currentTenantSetting, end}`
  interface.
- Changed no SQL, RLS policy, connection/pool setting, statement timeout, query caller, route, or
  traffic target.

## Red-first and focused proof

The ownership/import guard failed in two intended tests before the move. The canonical gate then
found a stale assurance citation; a new relocation assertion reproduced that failure before the
reference was corrected. Final focused proof:

```text
node --test \
  tests/node-api/module-relocation.test.mjs \
  tests/contract/boundary-references.test.mjs
# tests 18
# pass 18
# fail 0

node --test --test-concurrency=1 tests/node-api/db-tenant.test.mjs
# tests 8
# pass 8
# fail 0

pnpm --filter @quran-ai/server build
# exit 0

rg 'services/node-api/lib/db\.mjs|from "\.\.?/lib/db\.mjs"' \
  services server tests scripts packages
# no stale runtime/test imports

git diff --check
# exit 0
```

The live restricted-role oracle proved the stale-valid-context fail-open hazard, transaction-local
tenant context, no pool leak after success, no leak after a JavaScript throw, no leak after a
database error, isolation under interleaved tenants, invalid-tenant refusal, and missing-connection
refusal.

## Production-image proof

The source-built image reached healthy with Rust intentionally unreachable, ran as non-root, loaded
the relocated module together with its declared `postgres` dependency, and refused both invalid
connection strings:

```text
health=healthy
user=node
image=sha256:21f18bc9b0fee14620f28857b41018d222acb7870d4e4d6d6d6f59756302b06a
db export loaded; invalid connection strings refused
```

The exact temporary container was removed. This is local engineering evidence, not a release
digest.

## Canonical proof

The first post-move gate was correctly rejected because `specs/cutover/boundary.md` still cited the
deleted path. After correcting and pinning that consumer, the complete live gate passed:

```text
MIGRATION_TEST_ADMIN_URL=postgresql://hawzhin@127.0.0.1:5433/postgres bash scripts/verify.sh
# canonical Node tests 635; pass 633; fail 0; skipped 1; todo 1
# direct API parity tests 369; pass 369; fail 0
# through-Node API parity tests 319; pass 319; fail 0
VERIFY OK
```

W2.4 remains unchecked until all remaining legacy Node modules move and required remote CI is green.
