# W2.16 T1 evidence — device identity schema

**Local status:** complete on 2026-08-07<br>
**Umbrella status:** open pending T2–T5 and required remote CI

## Red proof

`tests/migrations/device-identity-migration.test.mjs` was added before migration 0035. Its first
live-Postgres run failed because the migration plan ended at 0034 and neither final-purpose device
identity relation existed.

## Implemented boundary

- Added checksum-pinned `infra/migrations/0035_device_identity.sql` and manifest entry
  `45fac1fcb38c89ffd19a3dd6c916514b63b0eaaa31e51c277609cf609e342aa1`.
- Added forced-RLS `device_enrollment_invitations` and generation-based `device_sessions` tables.
- Stored only credential hashes; constraints pin token-hash shape, expiries, state, family lineage,
  one active generation, and one successor per generation.
- Added hardened tenant-discovery functions for invitation, access-token, and refresh-token hash
  lookup. Each pins `search_path`, revokes `PUBLIC` execution, and is granted only to the restricted
  application role when present.
- Extended provisioning, SQL smoke inventory, migration convergence, restricted-role, checksum,
  and exact verify-invocation proofs.
- Preserved migration 0034 as immutable history while allowing 0035 to become the new plan tail.

## Focused proof

- Device migration proof: 2/2 passed against live Postgres.
- Migration runner, schema equivalence, restricted-role, and device migration group: 10/10 passed.
- Job-outbox history plus device migration: 4/4 passed.
- Verify invocation contract: 19/19 passed.
- SQL smoke: passed in rollback mode across 20 tenant tables, including both device relations.
- `git diff --check`: passed.

## Canonical proof

The exact repository gate ran with the live PostgreSQL administrative URL and PostgreSQL 16 client:

```sh
set -a
source scripts/stack.env
set +a
MIGRATION_TEST_ADMIN_URL='postgresql://hawzhin@127.0.0.1:5433/postgres' \
  PATH="/opt/homebrew/Cellar/postgresql@16/16.14/bin:$PATH" \
  bash scripts/verify.sh
```

Result: `VERIFY OK`. This included the full TypeScript/Node, Python, and Rust suites; live migration,
RLS, platform API, parity, retained-audio/privacy, and provenance integration proofs; production
build; bundle-secret and security-header guards; dependency-license policy; and model-evaluation
claim guards. The reported parity groups included 389 full tests and 332 Node-port tests, both with
zero failures.

Flutter verification was not applicable to T1 and no Flutter toolchain is installed in this
workspace. Remote CI cannot be observed from the current environment, so it is explicitly not
claimed green.
