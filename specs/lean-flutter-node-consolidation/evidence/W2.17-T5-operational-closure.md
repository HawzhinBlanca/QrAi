# W2.17 T5 evidence — operational closure

**Closure date:** 2026-08-08<br>
**Result:** local and exact-candidate remote proof passed<br>
**Scope:** approved W2.17 engineering boundary; W2.18 deployment evidence remains open

## Operational closure

- The living architecture, decisions, testing, data inventory, staging, threat-model,
  backup/restore, readiness, scholar-review, root README, and agent guidance now describe the
  worker-owned inference boundary and the single Node backend image.
- The unsafe orphan `scripts/privacy-audit-run.mjs` was retired. It parsed deployment environment
  files, embedded a personal tool path, changed database roles directly, and targeted the removed
  standalone ML topology. The maintained privacy proof is `scripts/smoke-privacy.mjs`, and a
  contract guard pins the orphan's absence.
- The API's wait-only durable-job helper is now named `waitForJobResult` in
  `server/src/jobs/wait-for-job.mjs`; the old `runInlineJob`/`jobs/inline.mjs` naming no longer
  implies that the API may execute inference.
- Importing `server/src/inference/runtime.mjs` no longer allocates a second production object store
  or creates local audit directories. The fallback store is lazy, the worker-injected store remains
  the production owner, and subprocess tests prove import-time filesystem/storage silence.
- Deterministic Tajweed text rules are documented as sourced instructional annotations without
  invented confidence, severity, or review state. Acoustic learner-performance findings remain a
  distinct gated surface.

## Focused proof

- W2.17 focused aggregate: 42 passed, 0 failed.
- Acoustic/import/storage regression set: 13 passed, 0 failed.
- Server and contracts typechecks passed.
- `node scripts/smoke-ml.mjs` passed against the worker composition.
- `node scripts/smoke-privacy.mjs` passed against the worker composition.
- Strict `docker compose config --quiet` passed with required configuration supplied; the resolved
  topology contains no `ml-inference` service.
- `git diff --check` passed after the canonical run.
- Hygiene search found no old `runInlineJob` or `jobs/inline.mjs` use and only the intentional
  absence assertion for `scripts/privacy-audit-run.mjs`.

## Canonical command

```sh
set -a
source scripts/stack.env
set +a
MIGRATION_TEST_ADMIN_URL='postgresql://hawzhin@127.0.0.1:5433/postgres' \
  PATH="/opt/homebrew/Cellar/postgresql@16/16.14/bin:$PATH" \
  bash scripts/verify.sh
```

Final output: `VERIFY OK` (exit 0). The successful run included live Postgres migration and RLS
proof, 99 platform API integration tests, real gateway/audio processes, retained-audio repair,
model-provenance round trips, 389 cross-runtime parity tests, 332 tests through the Node port,
production builds, secret/header/licence guards, and model-claim validation.

The first canonical attempt correctly caught one stale acoustic static assertion left by the lazy
store composition change. The assertion was updated to require the explicit fallback and propagation
into acoustic shadow analysis; the focused regression set passed before the full canonical rerun.

## Refreshed closure proof

The current server-owned inference/worker boundary was rerun on 2026-08-08 in two focused groups.
The core module, compatibility, inference, wait-only, and cancellation group reported 165 tests:
163 passed, one intentional live-ASR skip, one declared scholar-review Tajweed TODO, and zero
failures. The acceptance-oriented group then exercised real-audio finalization, provenance,
durable workflows, object storage, teacher playback/repair, retention, API waiting, cancellation,
and worker ownership:

```text
DATABASE_URL=postgresql://hawzhin@127.0.0.1:5433/quran_ai \
MIGRATION_TEST_ADMIN_URL=postgresql://hawzhin@127.0.0.1:5433/quran_ai \
  node --experimental-strip-types --test --test-concurrency=1 \
    tests/jobs/local-inference-worker.test.mjs \
    tests/jobs/api-job-wait.test.mjs \
    tests/jobs/inference-cancellation.test.mjs \
    tests/e2e/real-audio-finalize.test.mjs \
    tests/e2e/model-provenance-roundtrip.test.mjs \
    tests/e2e/durable-workflows.test.mjs \
    tests/e2e/audio-lifecycle.test.mjs \
    tests/e2e/teacher-audio-index.test.mjs \
    tests/inference/audio-retention-worker.test.mjs
# tests 28; pass 28; fail 0; skipped 0

MIGRATION_TEST_ADMIN_URL=postgresql://hawzhin@127.0.0.1:5433/quran_ai \
  bash scripts/verify.sh
# Node tests 831; pass 829; fail 0; skipped 1; todo 1
# direct API parity 389/389
# through-Node API parity 332/332
VERIFY OK
```

Exact source candidate `616f5be8bcf45640358cd71218354f79d4a9ba43` then passed all required
remote checks on PR #388: `node-min` in 57 seconds, `android` in 4 minutes 35 seconds, `build` in
4 minutes 55 seconds, and `verify` in 12 minutes 47 seconds. The remote verify job also passed the
deliberate parity mutation and CI smoke stages.

## Claims deliberately not made

- Flutter analysis and widget/unit tests passed locally; the live-stack Flutter test remained an
  explicit skip. This is not physical-device, signed native artifact, or release-build proof.
- Staging/production infrastructure, deployment observation, and human product/security/
  religious-domain sign-offs remain W2.18 and later release obligations.
- No fixture, deterministic rule count, or local test result is presented as model accuracy,
  production reliability, or evidence that the product is already number one.
