# W2.4 slice 2 evidence — relocate the metrics leaf

**Status:** engineering proof complete; W2.4 remains open for the remaining modules and remote CI<br>
**Date:** 2026-08-07

## Scope

- Moved `services/node-api/lib/metrics.mjs` to `server/src/lib/metrics.mjs`.
- Updated all mapped external importers: the server composition shell, the infrastructure route,
  and the direct metrics contract test.
- Preserved the exact four-export surface (`LATENCY_BUCKETS_MS`, `escape`, `createMetrics`, and
  `metricsAccessAllowed`) plus histogram buckets, label escaping, Prometheus bytes, and fail-closed
  token precedence.
- Deleted the old path. No route was cut over and the Rust compatibility proxy remains available.

## Red-first and focused proof

The two new relocation assertions failed before implementation because the old path existed and all
three callers still named it. After the move:

```text
node --test tests/node-api/module-relocation.test.mjs \
  tests/node-api/metrics-render.test.mjs \
  tests/node-api/shell.test.mjs \
  tests/node-api/routes-table.test.mjs \
  tests/node-api/production-image.test.mjs
# tests 41
# pass 41
# fail 0

pnpm --filter @quran-ai/server build
# exit 0

rg 'services/node-api/lib/metrics\\.mjs|from "\\.\\.?/lib/metrics\\.mjs"' \
  services tests scripts server
# no stale references

git diff --check
# exit 0
```

## Production-image proof

The source-built image reached healthy with only `GET /health` local and an intentionally
unreachable Rust upstream. The probe imported the moved module, recorded one request, and rendered
the Prometheus counter inside the container:

```text
health=healthy
user=node
image=sha256:5bc5af891ce66ea4f8304912706caa3cf4dc507a09c893b5fbeac511d48db3cc
moved-metrics=exercised
```

The temporary container was removed. This is local engineering evidence, not a release digest.

## Canonical proof

```text
MIGRATION_TEST_ADMIN_URL=postgresql://hawzhin@127.0.0.1:5433/postgres bash scripts/verify.sh
# focused metrics/relocation tests 41; pass 41; fail 0
# direct API parity tests 369; pass 369; fail 0
# through-Node API parity tests 319; pass 319; fail 0
VERIFY OK
```

W2.4 stays unchecked until every legacy Node module has moved and required remote CI is green.
