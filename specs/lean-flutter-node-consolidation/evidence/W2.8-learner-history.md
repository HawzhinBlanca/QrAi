# W2.8 evidence — learner-owned recitation history

**Status:** complete; local, image, and exact-candidate remote proof are green<br>
**Date:** 2026-08-08

## Delivered boundary

- `GET /v1/learner/recitation-sessions` is an explicit ADR-0038 Node target addition, separate
  from the tenant-wide teacher/admin/ops session list.
- The route is learner-only and returns only the authenticated learner's own tenant-scoped
  sessions, ordered by exact `(started_at DESC, id DESC)` keyset pagination.
- Each history row reports acoustic finding state counts only. It exposes no learner id, tenant id,
  tajweed judgement, confidence, explanation, or source; detailed feedback remains governed by the
  W2.7 review/evidence redaction gate.
- Cursor lookup is owner- and tenant-scoped. Unknown, other-owner, and cross-tenant cursors all
  return 404. `limit` is strict, integer-only, and bounded to `[1,50]` with a default of 20.
- The immutable Rust baseline remains 42 operations. The active transition contract is derived as
  that baseline plus manifest-classified implemented Node additions, currently 43 operations.
- No service, migration, table, queue, model, or duplicate learner-feedback policy was added.

## Red proof

Before implementation, the contract suite failed because learner history was still classified
`planned`, and the route registries remained at 40. Forcing the route local then failed shell startup:

```text
NODE_API_PORTED names an unportable route: GET /v1/learner/recitation-sessions
```

The first full gate exposed one additional hard-coded transition assumption: the older coverage
guard required OpenAPI to equal only the 42-route Rust baseline. It was tightened to require the
baseline plus only manifest-classified implemented additions, and to prove every implemented
addition exists in the executable Node route table.

## Focused green proof

- Learner-history live E2E: 5 passed, 0 failed.
- Contract/transition-inventory/route-registry focused suite: 46 passed, 0 failed.
- OpenAPI fixture validation: 12 covered live-response fixtures validated; 0 failures.
- `git diff --check`, Node syntax checks, server typecheck, and package build: passed through the
  canonical gate.

The live E2E proves:

- learner-own-only and staff-surface separation;
- stable keyset pages when a newer session arrives between page requests;
- unknown, other-owner, and cross-tenant cursor 404s;
- hostile limit rejection; and
- pending-to-reviewed refresh after a real teacher decision without rerunning inference, while the
  detailed finding remains fail-closed and redacted.

## Canonical proof

Command:

```sh
set -a
source scripts/stack.env
set +a
MIGRATION_TEST_ADMIN_URL='postgresql://hawzhin@127.0.0.1:5433/postgres' bash scripts/verify.sh
```

Result: `VERIFY OK`.

- Direct Rust-oracle API parity: 381 passed, 0 failed.
- Full through-Node portable-route parity: 331 passed, 0 failed.
- The dedicated learner-history live-Postgres stage: 5 passed, 0 failed.
- The same run passed Rust fmt/clippy, TypeScript checks, Web/package tests, Python ASR/evaluator
  tests, Rust unit/integration tests, live RLS/migration/storage/realtime tests, production builds,
  secret/security scans, licence policy, and model-evidence guards.

## Production image proof

- Image: `qrai-node-api:w28`
- Digest: `sha256:8f822226b65fed41bd34e92f3f8d86f59465dde23c65524aaa61a345d494da4e`
- Container state: `running healthy`, configured user `node`.
- Live probes: `/health` 200 `ok`; `/ready` 200 `ready` against live Postgres.
- In-container import probe:

```json
{"uid":1000,"routes":41,"portable":41,"learnerHistoryRoute":true,"handler":true,"legacy":false,"rustApi":false}
```

The temporary probe container was stopped and removed.

## Completion boundary

The route and parity counts above are the historical implementation-slice proof from 2026-08-07.
Later approved work added owner-gated device operations and expanded the executable registry to 44
operations without changing the learner-history contract. The current closure proof on 2026-08-08
was:

```text
DATABASE_URL=postgresql://hawzhin@127.0.0.1:5433/quran_ai \
  node --test --test-concurrency=1 tests/e2e/learner-history.test.mjs
# tests 5; pass 5; fail 0

node --experimental-strip-types --test \
  tests/api-parity/coverage.test.mjs \
  tests/contract/coverage.test.mjs \
  tests/contract/openapi-completeness.test.mjs \
  tests/contract/verify-invocations.test.mjs \
  tests/node-api/route-registry.test.mjs \
  tests/node-api/standalone.test.mjs
# tests 64; pass 64; fail 0

DATABASE_URL=postgresql://hawzhin@127.0.0.1:5433/quran_ai \
  node --test --test-concurrency=1 tests/api-parity/contract-shapes.test.mjs
# tests 8; pass 8; fail 0

MIGRATION_TEST_ADMIN_URL=postgresql://hawzhin@127.0.0.1:5433/quran_ai \
  bash scripts/verify.sh
# Node tests 831; pass 829; fail 0; skipped 1; todo 1
# direct API parity 389/389
# through-Node API parity 332/332
VERIFY OK
```

Exact source candidate `ab51521afafbdd882b75b9b09ad3a2b4e5527769` then passed all required
remote checks on PR #388: `verify`, `node-min`, `android`, and `build`. The remote verify job also
passed the mutation proof and CI smoke, while the image job passed its non-root and native Node
health checks.

The live journey proves own-only history and staff-surface separation, stable keyset pagination,
unknown/other-owner/cross-tenant cursor hiding, strict bounded limits, and a teacher decision
appearing on refresh without rerunning inference. This closes W2.8 without weakening W2.7's
detailed learner-feedback gate or claiming that later Flutter inbox work is complete.
