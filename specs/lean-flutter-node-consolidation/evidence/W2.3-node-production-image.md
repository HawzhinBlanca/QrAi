# W2.3 evidence — Node production image and internal shadow

**Status:** complete; canonical verification and required remote checks are green, and the ledger is closed<br>
**Date:** 2026-08-08

## Implemented image and topology

- `server/Dockerfile` is multi-stage and pins the official Node 22.13.1 Bookworm-slim OCI index to
  `sha256:83fdfa2a4de32d7f8d79829ea259bd6a4821f8b2d123204ac467fbe3966450fc`.
- The disposable builder pins Corepack 0.34.5 and pnpm 11.7.0, then deploys only the frozen
  `@quran-ai/server` production graph. The runtime receives the six declared production
  dependencies, package-owned server files, the reviewed route manifest, Quran provenance, and the
  shared acoustic-candidate declaration.
- The retired `services/node-api` deployment tree and deployable `services/ml-inference` source are
  absent. TypeScript, tests, Web sources, the Rust API, and broad Quran source trees are not copied.
- Runtime is `NODE_ENV=production`, runs as `USER node`, handles `SIGTERM`, and uses a native,
  bounded, fail-closed healthcheck without installing `curl` or another package manager.
- One `node-backend` image serves three isolated internal roles: `node-api`, `job-worker`, and
  `node-realtime`. None publishes a host port. The base Compose topology still routes Web HTTP to
  Rust and retains the Rust realtime gateway as the traffic owner.
- Release, rollback, signed-manifest, smoke, SBOM, licence, and Docker workflow inventories include
  the shared Node artifact. Six deployable image keys remain explicit; `node-backend` maps to all
  three Node process roles.

This task proves the immutable production artifact and its observation-only topology. It does not
claim an HTTP or realtime traffic cutover.

## Focused proof

These commands were rerun against the closing tree:

```text
node --test tests/node-api/standalone-lifecycle.test.mjs \
  tests/node-api/production-image.test.mjs
# tests 10
# pass 10
# fail 0

node --test scripts/release-images.test.mjs \
  scripts/release-build-evidence.test.mjs \
  scripts/release-manifest.test.mjs \
  scripts/smoke-evidence.test.mjs
# tests 38
# pass 38
# fail 0

node scripts/check-licenses.mjs --self-test
# check-licenses self-test OK (16 SPDX cases)

node scripts/check-licenses.mjs
# 235 JS + 317 Rust = 552 third-party packages
# 0 unapproved licences
```

`docker compose config --quiet` also exited zero with the six required secret variables supplied as
non-secret, process-local audit overrides. No `.env` file was changed.

The consolidated repository history does not expose a reproducible pre-image commit, so this
closure does not preserve the former unverified “5/5 red-first” assertion or an old local image ID.
The exact remote Docker workflow below is the current executable image proof.

## Canonical proof

The ledger updater reran the focused proof and the full live-Postgres canonical gate before changing
W2.3 from open to done:

```text
PATH=/Users/hawzhin/flutter/bin:$PATH \
MIGRATION_TEST_ADMIN_URL=postgresql://hawzhin@127.0.0.1:5433/quran_ai \
scripts/update-ledger.sh W2.3 \
  "tests/node-api/production-image.test.mjs scripts/release-manifest.test.mjs"
VERIFY OK
Ledger updated: W2.3 done (tests: tests/node-api/production-image.test.mjs scripts/release-manifest.test.mjs; verify.sh passed).
```

Required remote checks also passed on exact branch SHA
`6c331d56ef68ad96725f02df090bf6de8b4a2388`, where the W2.3 artifact and focused proofs are
unchanged:

- [`docker-build` run 31238661300](https://github.com/HawzhinBlanca/QrAi/actions/runs/31238661300)
  built the Compose graph, asserted non-root containers, passed the clean Node image smoke, proved
  fail-closed Web upstream rendering, and reached healthy through the native Node healthcheck;
- [`ci` run 31238661296](https://github.com/HawzhinBlanca/QrAi/actions/runs/31238661296) passed
  canonical verify, mutation, smoke, `node-min`, and `android`.

Only W2.3 is closed by this evidence. No traffic cutover or release promotion is claimed.
