# W2.4 slice 4 evidence — relocate the learner-feedback authority gate

**Status:** engineering proof complete; W2.4 remains open for the remaining modules and remote CI<br>
**Date:** 2026-08-07

## Scope

- Moved `services/node-api/lib/learner-feedback-gate.mjs` to
  `server/src/lib/learner-feedback-gate.mjs` and removed the old owner.
- Updated both runtime consumers (`review` and `ml-proxy`), the direct cross-language contract
  import, and both source-policy reads in that contract.
- Preserved the single export and the complete authority decision: acoustic basis, reviewed status,
  inclusive 0.82 confidence floor, complete sources/spans/ids/digests, available audio, calibrated
  status, release-trusted evidence, and explicit non-withholding.
- Changed no learner-facing response policy, route, traffic target, or canonical data.

## Red-first and focused proof

The new ownership and caller guards failed before the implementation. After the move:

```text
node --experimental-strip-types --test \
  tests/node-api/module-relocation.test.mjs \
  tests/contract/tajweed-gate-parity.test.mjs \
  tests/contract/learner-feedback-gate.test.mjs \
  tests/contract/tajweed-analysis-basis.test.mjs \
  tests/contract/no-invented-confidence.test.mjs
# tests 23
# pass 23
# fail 0

node --test --test-concurrency=1 \
  tests/api-parity/review-parity.test.mjs \
  tests/api-parity/ml-asr-proxy-parity.test.mjs \
  tests/api-parity/ml-proxy.test.mjs
# tests 49
# pass 49
# fail 0

pnpm --filter @quran-ai/server build
# exit 0

rg 'services/node-api/lib/learner-feedback-gate\\.mjs|from "\\.\\.?/lib/learner-feedback-gate\\.mjs"' \
  services tests scripts server
# no stale references
```

## Production-image proof

The source-built non-root image reached healthy with Rust intentionally unreachable. The in-image
probe evaluated a complete record at confidence `0.82` as allowed, then changed only its review
status to `ai-suggested` and proved it was withheld:

```text
health=healthy
user=node
image=sha256:bfa4afccdbf81ca5c39f2fd82f59c57cf3346c67a2de186c41e98e28b0deb9a8
moved-learner-gate=positive-and-negative
```

The temporary container was removed. This is local engineering evidence, not a release digest.

## Canonical proof

```text
MIGRATION_TEST_ADMIN_URL=postgresql://hawzhin@127.0.0.1:5433/postgres bash scripts/verify.sh
# targeted live review/ML tests 49; pass 49; fail 0
# direct API parity tests 369; pass 369; fail 0
# through-Node API parity tests 319; pass 319; fail 0
VERIFY OK
```

W2.4 remains unchecked until all remaining legacy Node modules move and required remote CI is green.
