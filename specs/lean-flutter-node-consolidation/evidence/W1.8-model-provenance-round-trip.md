# W1.8 — model provenance round trip

Status: COMPLETE; focused, canonical, and exact-candidate remote proof are green<br>
Implementation date: 2026-08-07<br>
Closure date: 2026-08-08<br>
Scope: engineering proof only; the captured CC0 recitation remains integration-fixture-only and
does not evaluate or select an ASR model.

## Result

The private finalize chain now preserves the ASR (and optional forced-aligner) component records,
adds the exact Quran-aligner record, and persists that complete document once per finalized session.
Every evidence-backed word links to the run through `(alignment_run_id, tenant_id, session_id)`.
Public Rust/Node proxies reject both client-authored measured spans and client-authored transcript
attribution. A structurally valid producer whose compatibility model disagrees with the session is
refused with zero word and run writes.

The staff-only alignment read returns the stored transcript source, compatibility model, component
document, dataset, evidence ids, and audit id. Legacy/client rows return null attribution/dataset and
an empty evidence-id array. Learners receive 403; a foreign tenant receives no rows.

## Focused proof

- ML attribution/transcript/alignment: 49/49 passed.
- Rust/Node public ML/ASR boundary parity: 14/14 passed.
- Migration runner, convergence, registry, FK, and new-row constraint: 6/6 passed.
- Rust live-Postgres finalize cases: 5/5 passed, including model mismatch and atomic rollback.
- Stored alignment contract/read parity: 21/21 passed.
- Session-write/effect parity: 24/24 passed.
- Actual ML + captured-real-ASR + Rust + live-Postgres E2E: 2/2 passed.
- Rust integration coverage ledger: 99/99 tests accounted for; the unported Node finalizer remains
  an explicit W2.6 deferral rather than false parity.

## Exact round-trip assertions

The observed Quran producer reported:

- compatibility model: `quran-constrained-levenshtein@1`;
- primary component: `quran-aligner`;
- component order: `asr`, `quran-aligner`;
- real dataset: `alquran-cloud:quran-uthmani:full-quran-2026-06-26`;
- one non-empty response evidence id;
- 15 linked word rows (11 matched, 4 misread) sharing exactly one run.

`tests/e2e/model-provenance-roundtrip.test.mjs` compared the producer JSON directly with the
database JSON and every restricted readback row. No inferred/default attribution participates in
that comparison.

## Canonical gate

The final local gate ran against live Postgres with:

`DATABASE_URL=postgresql://hawzhin@127.0.0.1:5433/quran_ai MIGRATION_TEST_ADMIN_URL=postgresql://hawzhin@127.0.0.1:5433/quran_ai bash scripts/verify.sh`

It exited zero with `VERIFY OK`. This included 99/99 live platform integration tests, both complete
Rust/Node parity passes, the two actual-ML provenance E2Es, migration convergence, builds, secret and
security-header guards, dependency-license checks, and model-evaluation-claim checks.

The first full run correctly caught one old weekly-progress fixture that labeled rows
`server-derived` without a run. The fixture now creates one declared-fixture run, inherits the
session's selected model, and links all three measured rows. Its focused live-Postgres regression
and the final 99-test integration run both pass.

## Refreshed closure proof

The current attribution, transcript-composition, proxy-boundary, and restricted-readback surface
was rerun on 2026-08-08 against the real Rust binary and live Postgres:

```text
cargo build --manifest-path services/platform-api/Cargo.toml
DATABASE_URL=postgresql://hawzhin@127.0.0.1:5433/quran_ai \
MIGRATION_TEST_ADMIN_URL=postgresql://hawzhin@127.0.0.1:5433/quran_ai \
  node --experimental-strip-types --test --test-concurrency=1 \
    tests/inference/model-attribution.test.mjs \
    tests/inference/session-transcript.test.mjs \
    tests/api-parity/ml-asr-proxy-parity.test.mjs \
    tests/e2e/model-provenance-roundtrip.test.mjs
# tests 41; pass 41; fail 0; skipped 0
```

Exact source candidate `ec3e1b711e640555bfafa0f41e3689dae77b7934` passed every required
remote check on PR #388: `node-min` in 56 seconds, `android` in 4 minutes 52 seconds, `build` in
5 minutes 2 seconds, and `verify` in 12 minutes 51 seconds. The remote verify job also passed its
CPU-only forced-align dependency, database migration/provisioning, full Quran seed, dependency and
SBOM gates, canonical repository gate, deliberate API-parity mutation, and CI smoke stages. The
guarded ledger update then reran the complete local gate with live Postgres.
