# W2.17 T4 evidence — one Node image and retired standalone ML service

**Date:** 2026-08-07<br>
**Result:** local canonical proof passed (`VERIFY OK`)<br>
**Scope:** W2.17 T4 only; the W2.17 umbrella and required remote CI remain open.

## Implemented cutover

- `node-api` and `job-worker` now run from the exact same `server/Dockerfile` image identity; the
  worker owns local inference plus its private compatibility listener.
- Platform API, Node API, and realtime gateway ML traffic is pointed at the worker listener. The
  explicit compatibility allowlist remains key-gated, rate-limited, deadline-bounded, and limited
  to measured Rust/audio/privacy consumers.
- The standalone `ml-inference` Compose service, Dockerfile, launcher, release-image entry, and
  tracked source tree were removed. Ignored local audit/runtime data was deliberately preserved.
- Release manifests, image discovery, build evidence, smoke evidence, Docker CI, load defaults,
  OpenAPI descriptions, and production-image guards now describe one Node backend image.
- ML/privacy standalone smoke runs exercise the same worker handler/runtime/store composition;
  the full smoke orchestrator starts the real worker entrypoint.

## Focused proof

- Worker compatibility, contract, rate-limit, overwrite, privacy, and lifecycle suites passed.
- Production-image and release image/build/manifest/smoke-evidence suites passed.
- `docker compose config --quiet` passed with required configuration supplied.
- `node scripts/smoke-ml.mjs` passed.
- `node scripts/smoke-privacy.mjs` passed.
- The exact hermetic Node aggregate passed: 724 passed, 0 failed, 26 explicit skips, 1 existing
  TODO.

The first canonical attempt exposed one stale static test regex that assumed the retired standalone
function signature. It was corrected to require the new injected audio-store parameter, and its
focused test plus the exact aggregate both passed before the canonical rerun.

## Canonical command

```sh
set -a
source scripts/stack.env
set +a
MIGRATION_TEST_ADMIN_URL='postgresql://hawzhin@127.0.0.1:5433/postgres' \
  PATH="/opt/homebrew/Cellar/postgresql@16/16.14/bin:$PATH" \
  bash scripts/verify.sh
```

Final output: `VERIFY OK` (exit 0). The run included live Postgres migrations and tenant isolation,
private audio/privacy flows, real gateway processes, 389 Rust/Node parity checks, 332 through-Node
parity checks, production builds, security/licence guards, and model-evidence checks.
