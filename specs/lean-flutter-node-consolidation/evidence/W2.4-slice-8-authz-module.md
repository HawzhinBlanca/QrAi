# W2.4 slice 8 evidence — relocate the authorization authority

**Status:** engineering proof complete; W2.4 remains open for route/entrypoint relocation and remote CI<br>
**Date:** 2026-08-07

## Scope

- Moved `services/node-api/lib/authz.mjs` to `server/src/lib/authz.mjs` and removed the old owner.
- Updated the server entrypoint, all twelve route importers, the direct oracle, and three assurance
  citations.
- Preserved all ten exports, including the shared `ApiError`/`RejectionError` class identities.
- Changed no role, tenant, credential source/precedence, cookie, Origin/CSRF, expiry/idle-roll,
  response message/content type, route, or traffic target.

## Red-first and focused proof

The authority-ownership and all-caller/reference guards failed in exactly two tests before the move.
After implementation:

```text
node --test \
  tests/node-api/module-relocation.test.mjs \
  tests/node-api/authz.test.mjs \
  tests/node-api/shell.test.mjs \
  tests/node-api/no-secret-logging.test.mjs \
  tests/node-api/boot-guard.test.mjs \
  tests/contract/boundary-references.test.mjs \
  tests/node-api/production-image.test.mjs \
  tests/node-api/standalone-lifecycle.test.mjs
# tests 88
# pass 88
# fail 0

node --test --test-concurrency=1 \
  tests/api-parity/auth-token-parity.test.mjs \
  tests/api-parity/pilot-auth-parity.test.mjs \
  tests/api-parity/pilot-routes-parity.test.mjs \
  tests/api-parity/authz-matrix.test.mjs
# tests 76
# pass 76
# fail 0

pnpm --filter @quran-ai/server build
# exit 0

rg 'services/node-api/lib/authz\.mjs|from "\.\.?/lib/authz\.mjs"' \
  services server tests scripts packages specs/cutover specs/node-backend-port
# no stale scoped references

git diff --check
# exit 0
```

The focused/live proofs cover ownership on malformed values, every role gate, bearer/header/cookie
precedence, missing claims, `alg:none`, wrong secret, cookie parsing, Origin and CSRF, idle-roll,
expiry, invitation/session lifecycle, exact cookie attributes, generic refusals, and secret/log
redaction.

## Production-image proof

The source-built image reached healthy with Rust intentionally unreachable, ran as non-root,
preserved cross-module `ApiError` identity, accepted a valid ownership check, and rejected both
degenerate ownership and an unsigned JWT:

```text
health=healthy
user=node
image=sha256:4b2958c35e2b783b387f005cc4b6911d789fd56a49cc645e18af92a4fe9c01ea
authorization ownership and unsigned-token refusals passed
```

The exact temporary container was removed. This is local engineering evidence, not a release
digest.

## Canonical proof

```text
MIGRATION_TEST_ADMIN_URL=postgresql://hawzhin@127.0.0.1:5433/postgres bash scripts/verify.sh
# canonical Node tests 639; pass 637; fail 0; skipped 1; todo 1
# direct API parity tests 369; pass 369; fail 0
# through-Node API parity tests 319; pass 319; fail 0
VERIFY OK
```

All shared modules now live under `server/src/lib`; `services/node-api/lib` has no files. W2.4 stays
unchecked until the transitional routes/entrypoint move and required remote CI is green.
