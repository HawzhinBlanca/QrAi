# W1.9 — Tajweed instruction/performance boundary

Status: COMPLETE; focused, canonical, and exact-candidate remote proof are green<br>
Implementation date: 2026-08-07<br>
Closure date: 2026-08-08<br>
Scope: engineering correctness and reliability proof. This slice does not claim that an acoustic
Tajweed evaluator exists or that learner-performance quality has been measured.

## Result

Deterministic Quran-text rules are now instructional annotations, not findings about a learner.
The producer returns them in `annotations[]` with `analysisBasis='text-rule'`,
`instructional=true`, and sources. It omits confidence, severity, and review state. `findings[]` is
reserved for span-linked acoustic judgments and remains empty until W1.10 supplies a real acoustic
producer.

Rust and Node validate this disjoint envelope before using it. They reject text rules in
`findings[]`, acoustic items in `annotations[]`, and annotations carrying performance fields. Only
validated acoustic findings can enter `tajweed_findings`, teacher review, session feedback, or the
learner gate. The Flutter and web models require `analysisBasis='acoustic'` for a performance
finding. The agents runtime now mirrors the same refusal, closing the final shared-gate bypass found
by the canonical run.

Migration 0030 reclassifies historical `canonical-text` rows as `text-rule`, replaces their
placeholder confidence with null, and enforces the database invariant:

- text-rule implies null confidence;
- acoustic implies non-null confidence;
- review queues and learner-performance reads include acoustic rows only;
- accepting a text rule as learner-performance feedback is refused.

The migration was applied to the local canonical database: one new migration applied, 29 total.
No historical row was deleted.

## Focused proof

- Exact instruction/performance and no-invented-confidence contracts: 8/8 passed.
- Migration pinning, concurrency, convergence, and 0030 constraints: 8/8 passed.
- Rust/Node review parity, including text-rule acceptance refusal: 28/28 passed.
- Live database endpoint plus real-producer trace regressions: 36/36 passed.
- Agents shared-corpus mirror: 23/23 passed.
- TypeScript/Dart learner-gate parity: 5/5 passed.
- Rust platform library tests: 47/47 passed.

The trace regression proves that real text analysis produces sourced instructional annotations,
carries the server-seen trace into the ML audit, and creates zero performance rows and zero
`ml.tajweed.persisted` claims. Declared golden-fixture decimals are likewise stripped before the
instruction contract.

## Canonical gate

The final local gate ran against live Postgres with:

`DATABASE_URL=postgresql://hawzhin@127.0.0.1:5433/quran_ai MIGRATION_TEST_ADMIN_URL=postgresql://hawzhin@127.0.0.1:5433/quran_ai bash scripts/verify.sh`

It exited zero with `VERIFY OK`. The run included:

- TypeScript suites: contracts 36/36, Quran data 52/52, web 197/197;
- Node services: 549 cases, 547 passed, one intentional skip, one known TODO, zero failures;
- live platform integration: 99/99 passed;
- complete Rust-backed API/database/observability parity: 367/367 passed;
- every portable route through the Node port: 317/317 passed;
- migration, real-process WebSocket/audio, provenance E2Es, builds, secrets, security headers,
  dependency licenses, and model-evaluation-claim guards.

The full gate first exposed stale teacher-review/observability fixtures that treated canonical rules
as performance findings; those fixtures now use declared acoustic evidence or assert zero
performance persistence. A later captured run exposed the separate agents gate ignoring
`analysisBasis`; that runtime now refuses explicit non-acoustic input and agrees with the shared
corpus. The final complete run is green after both corrections.

## Refreshed closure proof

The current producer, contract, migration, persistence, review, and trace boundary was rerun on
2026-08-08 against the real Rust binary and live Postgres:

```text
cargo build --manifest-path services/platform-api/Cargo.toml
DATABASE_URL=postgresql://hawzhin@127.0.0.1:5433/quran_ai \
MIGRATION_TEST_ADMIN_URL=postgresql://hawzhin@127.0.0.1:5433/quran_ai \
  node --experimental-strip-types --test --test-concurrency=1 \
    tests/inference/tajweed.test.mjs \
    tests/contract/tajweed-analysis-basis.test.mjs \
    tests/contract/no-invented-confidence.test.mjs \
    tests/contract/ml-findings-shape.test.mjs \
    tests/api-parity/tajweed-persistence-effects.test.mjs \
    tests/api-parity/review-parity.test.mjs \
    tests/observability/trace-join.test.mjs \
    tests/migrations/migration-runner.test.mjs \
    tests/migrations/schema-equivalence.test.mjs \
    tests/migrations/restricted-role.test.mjs
# tests 63; pass 62; fail 0; skipped 0; todo 1

cd apps/flutter
flutter test test/tajweed_gate_test.dart
# tests 11; pass 11; fail 0
```

The single Node TODO is the pre-existing scholar-review marker for mushaddad ghunnah rule coverage;
it does not weaken the tested instruction/performance separation. Exact source candidate
`f038ef3e24243791ea2b8efa579e2298987c37a9` passed every required remote check on PR #388:
`node-min` in 53 seconds, `android` in 4 minutes 17 seconds, `build` in 4 minutes 41 seconds, and
`verify` in 13 minutes 3 seconds. The remote verify job also passed the canonical repository gate,
deliberate API-parity mutation, and CI smoke stages. The guarded ledger update then reran the
complete local gate with live Postgres.

## Honest limits

The focused Flutter learner gate passed 11/11 and the canonical Flutter suite runs locally and in
CI. This remains simulator/unit proof, not a physical-device, signed-release, or production-runtime
claim; the live-stack Flutter test remains an explicit environment-dependent skip.

No acoustic Tajweed model, Kurdish ASR quality result, teacher agreement result, or product-quality
score is claimed by W1.9. Those require W1.10–W1.13 real evaluation work.
