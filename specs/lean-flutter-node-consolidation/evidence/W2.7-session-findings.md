# W2.7 evidence — learner session Tajweed findings

Date: 2026-08-07

## Delivered boundary

- `GET /v1/recitation-sessions/{id}/tajweed-findings` is served by the package-owned Node route
  table and literal portable allowlist; the local production inventory is now 40 routes.
- The handler preserves the Rust oracle's tenant transaction, 404-before-ownership order,
  learner/teacher/admin/ops matrix, scholar refusal, acoustic-only query, confidence/id ordering,
  learner redaction, and full staff projection.
- Stored confidence is an ordinary number at the learner-feedback policy boundary and becomes
  `RustF64` only when serialized. This fixes and pins a fail-closed policy/wire type defect.
- No service, schema, migration, model, database view, or duplicate gate was added.

## Red proof

Before registration/implementation, forcing the route local failed shell startup with:

```text
NODE_API_PORTED names an unportable route:
GET /v1/recitation-sessions/{id}/tajweed-findings
```

The first absolute role-matrix run also rejected a brittle permanent-seed assumption: the live
database had no recitation-session seed, so scholar received 404 before authorization. The final
matrix creates and removes a declared learner-owned fixture and proves scholar receives 403.

## Focused green proof

- Absolute authorization matrix: 41 passed, 0 failed.
- Live database endpoints through the Node route: 36 passed, 0 failed, including byte-for-byte
  learner and teacher comparison against Rust.
- Contract/OpenAPI/Flutter/coverage checks: 82 passed, 0 failed.
- Route relocation/registry/standalone lifecycle checks: 32 passed, 0 failed.
- Server package build and `git diff --check`: passed.

## Canonical proof

Command:

```sh
set -a
source scripts/stack.env
set +a
MIGRATION_TEST_ADMIN_URL='postgresql://hawzhin@127.0.0.1:5433/postgres' bash scripts/verify.sh
```

Result: `VERIFY OK`.

- Direct Rust-oracle API parity: 380 passed, 0 failed.
- Full through-Node portable-route parity: 330 passed, 0 failed.
- The same run passed Rust fmt/clippy, TypeScript checks, Web/package tests, Python ASR/evaluator
  tests, Rust unit/integration tests, live RLS/migration/storage/realtime tests, production builds,
  security scans, licence policy, and model-evidence guards.

## Production image proof

- Image: `qrai-node-api:w27`
- Digest: `sha256:16297cf59c2ce4d94d440ea67b4dc9cb9342084bb5a028c95c8322c3b95d4186`
- Container state: `running healthy`, configured user `node`.
- Live probes: `/health` 200 `ok`; `/ready` 200 `ready` against live Postgres.
- In-container import probe:

```json
{"uid":1000,"routes":40,"portable":40,"sessionFindingsRoute":true,"handler":true,"legacy":false,"rustApi":false}
```

The temporary probe container was stopped and removed.

## Completion boundary

This is local engineering evidence, not production-release evidence. W2.7 remains unchecked in the
ledger until the required remote CI checks are green.
